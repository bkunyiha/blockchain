<div align="left">

<details>
<summary><b>Chapter Navigation ▼</b></summary>

### Part I: Foundations & Core Implementation

1. <a href="../00-Quick-Start.md">Chapter 1: Quick Start</a>
2. <a href="../01-Introduction.md">Chapter 2: Introduction & Overview</a>
3. <a href="../bitcoin-blockchain/README.md">Chapter 3: Introduction to Blockchain</a>
4. <a href="../bitcoin-blockchain/whitepaper-rust/00-Bitcoin-Whitepaper-Summary.md">Chapter 4: Bitcoin Whitepaper</a>
5. <a href="../bitcoin-blockchain/whitepaper-rust/00-Bitcoin-Whitepaper-Rust-Encoding-Summary.md">Chapter 5: Bitcoin Whitepaper in Rust</a>
6. <a href="../bitcoin-blockchain/Rust-Project-Index.md">Chapter 6: Rust Blockchain Project</a>
7. <a href="../bitcoin-blockchain/primitives/README.md">Chapter 7: Primitives</a>
8. <a href="../bitcoin-blockchain/util/README.md">Chapter 8: Utilities</a>
9. <a href="../bitcoin-blockchain/crypto/README.md">Chapter 9: Cryptography</a>
10. <a href="../bitcoin-blockchain/chain/01-Domain-Model.md">Chapter 10: Domain Model</a>
11. <a href="../bitcoin-blockchain/chain/02-Blockchain-State-Management.md">Chapter 11: Blockchain State Management</a>
12. <a href="../bitcoin-blockchain/chain/03-Chain-State-and-Storage.md">Chapter 12: Chain State and Storage</a>
13. <a href="../bitcoin-blockchain/chain/04-UTXO-Set.md">Chapter 13: UTXO Set</a>
14. <a href="../bitcoin-blockchain/chain/05-Transaction-Lifecycle.md">Chapter 14: Transaction Lifecycle</a>
15. <a href="../bitcoin-blockchain/chain/06-Block-Lifecycle-and-Mining.md">Chapter 15: Block Lifecycle and Mining</a>
16. <a href="../bitcoin-blockchain/chain/07-Consensus-and-Validation.md">Chapter 16: Consensus and Validation</a>
17. <a href="../bitcoin-blockchain/chain/08-Node-Orchestration.md">Chapter 17: Node Orchestration</a>
18. <a href="../bitcoin-blockchain/chain/09-Transaction-To-Block.md">Chapter 18: Transaction to Block</a>
19. <a href="../bitcoin-blockchain/chain/10-Whitepaper-Step-5-Block-Acceptance.md">Chapter 19: Block Acceptance</a>
20. <a href="../bitcoin-blockchain/store/README.md">Chapter 20: Storage Layer</a>
21. <a href="../bitcoin-blockchain/net/README.md">Chapter 21: Network Layer</a>
22. <a href="../bitcoin-blockchain/node/README.md">Chapter 22: Node Orchestration</a>
23. <a href="../bitcoin-blockchain/wallet/README.md">Chapter 23: Wallet System</a>
24. <a href="../bitcoin-blockchain/web/README.md">Chapter 24: Web API Architecture</a>
25. <a href="../bitcoin-desktop-ui-iced/04.1-Desktop-Admin-UI-Iced.md">Chapter 25: Desktop Admin (Iced)</a>
26. <a href="../bitcoin-desktop-ui-iced/04.1A-Desktop-Admin-UI-Code-Walkthrough.md">25A: Code Walkthrough</a>
27. <a href="../bitcoin-desktop-ui-iced/04.1B-Desktop-Admin-UI-Update-Loop.md">25B: Update Loop</a>
28. <a href="../bitcoin-desktop-ui-iced/04.1C-Desktop-Admin-UI-View-Layer.md">25C: View Layer</a>
29. **Chapter 26: Desktop Admin (Tauri)** ← *You are here*
30. <a href="04.2A-Tauri-Admin-Rust-Backend.md">26A: Rust Backend</a>
31. <a href="04.2B-Tauri-Admin-Frontend-Infrastructure.md">26B: Frontend Infrastructure</a>
32. <a href="04.2C-Tauri-Admin-Frontend-Pages.md">26C: Frontend Pages</a>
33. <a href="../bitcoin-wallet-ui-iced/05.1-Wallet-UI-Iced.md">Chapter 27: Wallet UI (Iced)</a>
34. <a href="../bitcoin-wallet-ui-iced/05.1A-Wallet-UI-Code-Listings.md">27A: Code Listings</a>
35. <a href="../bitcoin-wallet-ui-tauri/05.2-Wallet-UI-Tauri.md">Chapter 28: Wallet UI (Tauri)</a>
36. <a href="../bitcoin-wallet-ui-tauri/05.2A-Tauri-Wallet-Rust-Backend.md">28A: Rust Backend</a>
37. <a href="../bitcoin-wallet-ui-tauri/05.2B-Tauri-Wallet-Frontend-Infrastructure.md">28B: Frontend Infrastructure</a>
38. <a href="../bitcoin-wallet-ui-tauri/05.2C-Tauri-Wallet-Frontend-Pages.md">28C: Frontend Pages</a>
39. <a href="../embedded-database/06-Embedded-Database.md">Chapter 29: Embedded Database</a>
40. <a href="../embedded-database/06A-Embedded-Database-Code-Listings.md">29A: Code Listings</a>
41. <a href="../bitcoin-web-ui/06-Web-Admin-UI.md">Chapter 30: Web Admin Interface</a>
42. <a href="../bitcoin-web-ui/06A-Web-Admin-UI-Code-Listings.md">30A: Code Listings</a>
### Part II: Deployment & Operations

43. <a href="../ci/docker-compose/01-Introduction.md">Chapter 31: Docker Compose Deployment</a>
44. <a href="../ci/docker-compose/01A-Docker-Compose-Code-Listings.md">31A: Code Listings</a>
45. <a href="../ci/kubernetes/README.md">Chapter 32: Kubernetes Deployment</a>
46. <a href="../ci/kubernetes/01A-Kubernetes-Code-Listings.md">32A: Code Listings</a>
### Part III: Language Reference

47. <a href="../rust/README.md">Chapter 33: Rust Language Guide</a>
### Appendices

48. <a href="../Glossary.md">Glossary</a>
49. <a href="../Bibliography.md">Bibliography</a>
50. <a href="../Appendix-Source-Reference.md">Source Reference</a>

</details>

</div>

---

<div align="right">

**[← Back to Main Book](../README.md#table-of-contents)**

</div>

---

# Chapter 26: Desktop Admin Interface (Tauri) — Architecture and Code Walkthrough

> **Prerequisites:**: This chapter assumes familiarity with the Web API (Chapter 24) and basic React/TypeScript concepts. If you read the Iced admin UI (Chapter 25), you will see the same blockchain features exposed through a different architecture — but this chapter is self-contained and can be read independently.

> **What you will learn in this chapter:**
> - Build the same admin functionality using Tauri 2 with a Rust backend and React frontend
> - Implement IPC commands that bridge Rust and TypeScript across the process boundary
> - Compare the Tauri architecture with the Iced MVU approach from the previous chapter
> - Use Zustand for state management and React Query for data fetching in the frontend

> **Framework Comparison:** This chapter builds the same admin interface as Chapter 25, but with Tauri 2 — a Rust backend connected to a React/TypeScript frontend via IPC. If you read Chapter 25 first, you will see exactly where the architectures diverge: state management, rendering, and the boundary between business logic and UI.

> **Scope:** This chapter covers the Tauri 2 admin application using React and TypeScript. We do not cover Tauri plugins, auto-update mechanisms, or distribution through platform app stores.

**What is Tauri?** Tauri is a framework for building desktop applications that pair a Rust backend with a web-technology frontend (HTML/CSS/JS). Unlike Electron, Tauri does not bundle Chromium — it uses the operating system's native webview, resulting in much smaller binaries (typically 5–24 MB vs. 150+ MB). The Rust side exposes *commands* via `#[tauri::command]`, which the frontend calls through an `invoke()` function over an IPC bridge. The result is a two-language architecture: Rust handles business logic and system access, while React handles the UI.

This chapter explains the `bitcoin-desktop-ui-tauri` crate as a Rust developer reads it: **how a Tauri 2 app bridges a Rust backend to a React/TypeScript frontend**, **how commands flow across the IPC boundary**, and **how the frontend calls our Bitcoin implementation's admin API** through that bridge.

Complete Rust backend coverage lives in Chapter 26A (config, models, services, commands), and the React frontend is covered in Chapters 26B (infrastructure) and 26C (pages).
- diagrams make the IPC boundary and data flow explicit

<div align="center">

**[← Chapter 25C: View Layer](../bitcoin-desktop-ui-iced/04.1C-Desktop-Admin-UI-View-Layer.md)** | **Chapter 26: Desktop Admin Interface (Tauri)** | **[Next: Chapter 26A: Rust Backend →](04.2A-Tauri-Admin-Rust-Backend.md)**

</div>

---

## What this UI is (in one sentence)

The Tauri desktop admin UI is a **two-language desktop application**: a Rust backend exposes 22 `#[tauri::command]` handlers that wrap our `bitcoin-api` crate, while a React/TypeScript frontend calls those handlers through Tauri's IPC bridge using `invoke()`.

The Tauri desktop admin UI consists of: `main` (Tauri app setup, state management, command registration), `BitcoinApiService::*` (service layer wrapping `AdminClient` HTTP calls), `commands::*` (22 Tauri command handlers across 6 modules), `commands.ts` → `invoke()` (TypeScript-to-Rust bridge), and React pages (18 views), components (14 shared), hooks (2 custom).

---

## Why Tauri? (Iced vs. Tauri — an architectural comparison)

In Chapter 25 we built the same admin dashboard with Iced — a pure-Rust, single-language MVU framework. Chapter 26 builds the identical **feature set** (blockchain info, wallet management, mining controls, health checks) with Tauri, which gives us a **Rust backend paired with a web frontend**. For detailed feature walkthroughs, see **Chapter 25** — both UI implementations expose the same admin operations.

This chapter focuses on the **architectural differences** and Tauri-specific patterns (IPC, React Query, Zustand). Both approaches have real trade-offs:

| Aspect | Iced (Chapter 25) | Tauri (this chapter) |
|--------|-------------------|----------------------|
| **Languages** | Rust only | Rust backend + TypeScript/React frontend |
| **UI paradigm** | MVU (Model-View-Update) | Component-based (React) with IPC bridge |
| **State management** | Single `AdminApp` struct | Zustand (client) + React Query (server) |
| **Async boundary** | Manual Tokio runtime in background thread (`spawn_on_tokio`) | Tauri handles async natively — `#[tauri::command]` functions are async |
| **Event vocabulary** | `Message` enum with ~50 variants | React Router routes + React Query cache keys |
| **Styling** | Iced built-in widgets (programmatic) | Tailwind CSS (declarative) |
| **Shared code** | `bitcoin-api` crate (Rust) | `bitcoin-api` crate (Rust) — same crate, same API calls |
| **Binary size** | Smaller (pure Rust) | Larger (embeds Chromium webview) |
| **Ecosystem** | Iced widgets only | Full npm ecosystem (React Query, Zod, Lucide, etc.) |

The key point: both apps call exactly the same `AdminClient` methods from the `bitcoin-api` crate. The difference is where the UI layer lives (Rust vs. browser) and how events cross the boundary (Iced `Message` vs. Tauri `invoke`).

> **Tip:** Both the Iced admin (Chapter 25) and this Tauri admin consume the same `bitcoin-api` crate for blockchain communication. This means the backend API is framework-agnostic — you could add a third frontend without changing any backend code.

---

## Diagram: Tauri IPC architecture (how data flows)

```text
┌─────────────────────────────────────────────────────────────────────────┐
│  TAURI APPLICATION                                                      │
│                                                                         │
│  ┌──────────────────────────┐    IPC     ┌────────────────────────────┐ │
│  │   React Frontend         │  invoke()  │   Rust Backend             │ │
│  │   (WebView / Chromium)   │ ────────>  │   (src-tauri/)             │ │
│  │                          │            │                            │ │
│  │  Page Component          │            │  #[tauri::command]         │ │
│  │    └─ useQuery()         │            │    └─ reads State<RwLock>  │ │
│  │       └─ commands.ts     │            │       └─ BitcoinApiService │ │
│  │          └─ invoke(name) │  <──────── │          └─ AdminClient    │ │
│  │             {payload}    │  Result<V> │             └─ HTTP call   │ │
│  └──────────────────────────┘            └─────────────┬──────────────┘ │
│                                                        │                │
└────────────────────────────────────────────────────────│────────────────┘
                                                         │ HTTP
                                                         v
                                              ┌─────────────────────┐
                                              │  Bitcoin API Server │
                                              │  (Chapter 24)       │
                                              │  :8080              │
                                              └─────────────────────┘
```

Every user action in the browser follows this path:

1. **React page** triggers a query or form submission
2. **`commands.ts`** calls `invoke('command_name', { args })` — this crosses the IPC bridge
3. **Tauri routes** to the matching `#[tauri::command]` Rust function
4. **Command handler** reads `ApiConfig` from shared `RwLock` state
5. **`BitcoinApiService`** creates an `AdminClient` and makes an HTTP request
6. **Result** flows back: `AdminClient` → service → command → IPC → React Query cache → UI re-render

---

## Diagram: file-to-responsibility map (what lives where)

```text
bitcoin-desktop-ui-tauri/
├── src-tauri/                              ← RUST BACKEND
│   ├── src/
│   │   ├── main.rs
│   │   │   → Tauri app boot, state + plugin + command registration
│   │   ├── config/mod.rs
│   │   │   → ApiConfig (base_url, api_key) with env var fallback
│   │   ├── models/mod.rs
│   │   │   → Data structs (WalletAddress, BlockSummary, etc.)
│   │   ├── services/bitcoin_api.rs
│   │   │   → BitcoinApiService: wraps AdminClient for all endpoints
│   │   └── commands/
│   │       → 22 #[tauri::command] handlers, 6 domain modules
│   │       ├── mod.rs                      → Re-exports all command functions
│   │       ├── blockchain.rs
│   │       │   → get_blockchain_info, get_latest_blocks, ...
│   │       ├── wallet.rs
│   │       │   → create_wallet, get_balance, send_transaction, ...
│   │       ├── transactions.rs
│   │       │   → get_mempool, get_all_transactions, ...
│   │       ├── mining.rs
│   │       │   → get_mining_info, generate_blocks
│   │       ├── health.rs
│   │       │   → health_check, liveness_check, readiness_check
│   │       └── settings.rs
│   │           → get_config, update_config, check_connection
│   ├── Cargo.toml
│   │   → Dependencies: tauri 2, bitcoin-api, tokio, serde
│   └── tauri.conf.json
│       → Window config, CSP, plugin permissions
```

The React/TypeScript frontend mirrors the Rust backend structure with components, pages, and hooks:

```text
├── src/                                    ← REACT/TYPESCRIPT FRONTEND
│   ├── main.tsx
│   │   → React bootstrap: QueryClient, BrowserRouter, StrictMode
│   ├── App.tsx
│   │   → Route definitions (18 routes) + provider nesting
│   ├── types/index.ts
│   │   → TypeScript interfaces matching Rust models
│   ├── lib/
│   │   ├── commands.ts
│   │   │   → invoke() wrappers for all 22 Tauri commands
│   │   └── utils.ts
│   │       → formatBTC, truncateHash, debounce, cn, ...
│   ├── store/useAppStore.ts
│   │   → Zustand store: theme, menu, status bar
│   ├── hooks/
│   │   ├── useInvoke.ts
│   │   │   → Generic Tauri command hook (loading/error/data)
│   │   └── useClipboard.ts
│   │       → Clipboard integration via Tauri plugin
│   ├── components/ (14 shared components)
│   │   ├── AppLayout.tsx, Sidebar.tsx, TopBar.tsx, StatusBar.tsx
│   │   ├── ConnectionBadge.tsx, DataCard.tsx, DataTable.tsx
│   │   ├── JsonViewer.tsx, ConfirmDialog.tsx, SettingsDialog.tsx
│   │   ├── CommandPalette.tsx, ThemeProvider.tsx, ShortcutProvider.tsx
│   │   └── Toast.tsx
│   └── pages/ (18 page components)
│       ├── blockchain/ (BlockchainInfoPage, LatestBlocksPage, ...)
│       ├── wallet/ (CreateWalletPage, WalletInfoPage, ...)
│       ├── transactions/ (MempoolPage, AllTransactionsPage, ...)
│       ├── mining/ (MiningInfoPage, GenerateBlocksPage)
│       └── health/ (HealthPage)
│
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

---

## How to read this chapter (so the code "clicks")

We split the walkthrough into three companion chapters. Choose based on your interest:

- **If you want to understand the Rust backend** (how Tauri commands work, how the service layer wraps `AdminClient`, how shared state is managed): read **[Chapter 26A: Rust Backend](04.2A-Tauri-Admin-Rust-Backend.md)** — it covers `main.rs`, `config/`, `models/`, `services/`, and all six `commands/` modules.

- **If you want to understand the frontend infrastructure** (how React Router, React Query, Zustand, and Tauri IPC fit together): read **[Chapter 26B: Frontend Infrastructure](04.2B-Tauri-Admin-Frontend-Infrastructure.md)** — it covers `main.tsx`, `App.tsx`, `types/`, `lib/`, `store/`, `hooks/`, and all 14 shared components.

- **If you want to see the page implementations** (how each feature screen calls commands and renders data): read **[Chapter 26C: Frontend Pages](04.2C-Tauri-Admin-Frontend-Pages.md)** — it covers all 18 page components across the five navigation sections.

- **If you already know Iced and want to compare**: start with the architecture diagram above, then read 17A to see how Tauri replaces the manual `spawn_on_tokio` pattern with built-in async commands.

## Mental model (one sentence)

This UI is a **thin Rust command layer** (22 functions that delegate to `AdminClient`) paired with a **React frontend** that uses React Query for caching and Zustand for UI state, with Tauri's IPC bridge (`invoke`) as the only connection between the two worlds.

---

## Key architectural patterns

### Pattern 1: The Tauri command pattern

Every Rust command follows the same three-step shape:

1. **Read the shared config** from `State<RwLock<ApiConfig>>`
2. **Delegate to `BitcoinApiService`** which creates an `AdminClient` and makes the HTTP call
3. **Return `Result<Value, String>`** — Tauri serializes this as JSON back to the frontend

Here is a representative example — `get_blockchain_info` — showing all three layers:

**Layer 1: Command handler** (`commands/blockchain.rs`)

```rust
use crate::config::ApiConfig;
use crate::services::BitcoinApiService;
use serde_json::Value;
use std::sync::RwLock;
use tauri::State;

#[tauri::command]
pub async fn get_blockchain_info(
    config: State<'_, RwLock<ApiConfig>>  // '_ elides the command lifetime
) -> Result<Value, String> {
    let cfg = config.read().map_err(|e| format!("Lock error: {}", e))?;
    BitcoinApiService::get_blockchain_info(&cfg).await
}
```

The `#[tauri::command]` attribute tells Tauri to register this function as a callable command. `State<'_, RwLock<ApiConfig>>` is Tauri's managed state — it injects the shared config automatically. We acquire a read lock (not a write lock, since we only need the URL and API key), then delegate to the service layer.

**Layer 2: Service** (`services/bitcoin_api.rs`)

```rust
pub struct BitcoinApiService;

impl BitcoinApiService {
    pub async fn get_blockchain_info(
        config: &ApiConfig
    ) -> Result<Value, String> {
        let client = AdminClient::new(&config.base_url, &config.api_key);
        match client.get_blockchain_info().await {
            Ok(response) => {
                if response.success {
                    Ok(json!(response.data))
                } else {
                    let err_msg = response.error.unwrap_or_else(
                        || "Unknown error".to_string()
                    );
                    Err(err_msg)
                }
            }
            Err(e) => Err(format!("API error: {}", e)),
        }
    }
}
```

Every service method creates a fresh `AdminClient`, makes the async call, and normalizes the response: extract `data` on success, extract the error message on failure. We create a new client per call (rather than holding a persistent connection) because the user can change `base_url` and `api_key` at runtime through the settings dialog.

**Layer 3: Frontend invocation** (`lib/commands.ts`)

```typescript
import { invoke } from '@tauri-apps/api/core';

export const commands = {
  getBlockchainInfo: () => invoke('get_blockchain_info'),
  // ... 21 more commands
};
```

The frontend calls `invoke('get_blockchain_info')` which crosses the Tauri IPC bridge, runs the Rust function, and returns the result as a JavaScript object. The command name string must exactly match the Rust function name.

### Pattern 2: Shared config with `RwLock`

The app needs a single `ApiConfig` (base URL + API key) that is:

- **Read** by every command handler (22 functions reading concurrently)
- **Written** only by `update_config` (when the user changes settings)
- **Thread-safe** because Tauri runs commands on multiple threads

We solve this with `RwLock<ApiConfig>` managed as Tauri state:

```rust
fn main() {
    let api_config = ApiConfig::default();

    tauri::Builder::default()
        // Tauri injects this into every command
        .manage(RwLock::new(api_config))
        // ...
}
```

The `RwLock` allows many concurrent readers but exclusive writers — perfect for a "read-often, write-rarely" config object. See **Chapter 26A** for the full `main.rs` walkthrough.

### Pattern 3: React Query for server state

On the frontend, we use TanStack React Query to manage all data from the Rust backend:

```typescript
const { data, isLoading, error } = useQuery({
  queryKey: ['blockchain-info'],
  queryFn: () => commands.getBlockchainInfo(),
  refetchInterval: 30_000,     // Re-poll every 30 seconds
  staleTime: 5 * 60 * 1000,   // Cache valid for 5 minutes
});
```

This gives us automatic caching, background refetching, loading states, and error handling — all without manual state management. Every page component follows this pattern. See **Chapter 26B** for the full React Query setup.

> **Note:** React Query works identically whether the data source is Tauri IPC (this chapter), Tauri IPC with local persistence (Chapter 28), or HTTP REST (Chapter 30). The hook pattern, cache keys, and invalidation strategy are the same — only the `queryFn` changes.

---

## The Iced comparison: what Tauri replaces

If you read Chapter 4, you saw how Iced requires manual wiring for async work:

| Iced pattern | Tauri equivalent |
|-------------|-----------------|
| `init_runtime()` — start Tokio in a background thread | Not needed — Tauri manages its own async runtime |
| `spawn_on_tokio(future)` — bridge sync MVU to async | Not needed — `#[tauri::command] async fn` is natively async |
| `Message::FetchXxx` → `Task::perform(...)` → `Message::XxxLoaded(Result)` | `invoke('command_name')` → `Promise<Result>` — one call, one result |
| `Message` enum (~50 variants for all events) | React Router routes + React Query cache keys (declarative) |
| `view(&AdminApp) -> Element<Message>` (Rust rendering) | JSX components with Tailwind CSS (browser rendering) |
| `AdminApp` struct (single state tree) | Zustand (UI state) + React Query (server state) |

The biggest difference: in Iced, every async operation requires a round-trip through the `Message` enum (request variant → async work → loaded variant → state update → re-render). In Tauri, the frontend calls `invoke()`, gets a Promise, and React Query handles the rest. The "event vocabulary" problem disappears entirely.

---

Together, the companion chapters walk through every file in:

- `bitcoin-desktop-ui-tauri/src-tauri/src/main.rs`
- `bitcoin-desktop-ui-tauri/src-tauri/src/config/mod.rs`
- `bitcoin-desktop-ui-tauri/src-tauri/src/models/mod.rs`
- `bitcoin-desktop-ui-tauri/src-tauri/src/services/bitcoin_api.rs`
- `bitcoin-desktop-ui-tauri/src-tauri/src/commands/*.rs` (6 modules)
- `bitcoin-desktop-ui-tauri/src/main.tsx`
- `bitcoin-desktop-ui-tauri/src/App.tsx`
- `bitcoin-desktop-ui-tauri/src/types/index.ts`
- `bitcoin-desktop-ui-tauri/src/lib/commands.ts`
- `bitcoin-desktop-ui-tauri/src/lib/utils.ts`
- `bitcoin-desktop-ui-tauri/src/store/useAppStore.ts`
- `bitcoin-desktop-ui-tauri/src/hooks/*.ts` (2 hooks)
- `bitcoin-desktop-ui-tauri/src/components/*.tsx` (14 components)
- `bitcoin-desktop-ui-tauri/src/pages/**/*.tsx` (18 pages)
- `bitcoin-desktop-ui-tauri/Cargo.toml`, `package.json`, `tauri.conf.json`, `vite.config.ts`

---

## Summary

- We built the same admin functionality using Tauri 2, combining a Rust backend with a React/TypeScript frontend.
- We implemented IPC commands that bridge Rust and TypeScript, comparing this architecture with Iced's pure Rust approach.
- We used Zustand for state management and React Query for data fetching in the frontend layer.
- We saw how both implementations consume the same bitcoin-api crate, proving the backend API is framework-agnostic.

In the next chapter, we shift from admin interfaces to user-facing wallet applications, starting with the Iced wallet that adds encrypted persistence and a complete send/receive workflow.

> **Companion Chapter:** Complete source code for this chapter is available in: [17A: Rust Backend](04.2A-Tauri-Admin-Rust-Backend.md), [17B: Frontend Infrastructure](04.2B-Tauri-Admin-Frontend-Infrastructure.md), and [17C: Frontend Pages](04.2C-Tauri-Admin-Frontend-Pages.md). Read them for the full IPC command and React component implementations. In the print edition, these listings appear in the Appendix: Source Reference.

---

## Exercises

1. **Add a Tauri IPC Command** — Create a new Tauri command `get_difficulty` that calls the blockchain API and returns the mining difficulty. Register it in the Tauri builder, create the TypeScript invoke call, and display the result in a React component. Compare the effort with adding the same feature in Iced (Chapter 25).

2. **Framework Comparison** — Compare the Iced admin (Chapter 25) and Tauri admin side by side. For three specific features (state management, API calls, UI rendering), document the architectural differences. Which approach results in less code? Which is easier to test?


## Further Reading

- **[Tauri 2 Documentation](https://v2.tauri.app/)** — Official Tauri guides and API reference.
- **[Zustand Documentation](https://zustand-demo.pmnd.rs/)** — The lightweight state management library used in the frontend.
- **[TanStack Query (React Query)](https://tanstack.com/query/)** — Data fetching and caching library.

---

<div align="center">

**[← Chapter 25C: View Layer](../bitcoin-desktop-ui-iced/04.1C-Desktop-Admin-UI-View-Layer.md)** | **Chapter 26: Desktop Admin Interface (Tauri)** | **[Chapter 26A: Rust Backend →](04.2A-Tauri-Admin-Rust-Backend.md)**

</div>

---
